package visao;

public class Mensagem extends Exception {
    //Exibe as mensagens de alguns rys e catchs ads outras classes
    public Mensagem(String mensagem) {
        super(mensagem);
    }
}
